services:
    _defaults:
        autowire:      true
        autoconfigure: true
        public:        false

    Mmi\EventListener\KernelRequestSessionWarmer:
        tags:
            - { name: "kernel.event_listener", event: "kernel.request", method: "warmUpSession" }
